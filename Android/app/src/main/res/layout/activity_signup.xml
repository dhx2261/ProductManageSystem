<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".Activity_Signup">

    <EditText
        android:id="@+id/signupusername"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="80dp"
        android:hint="user name"
        android:textSize="30dp"
        android:layout_marginRight="100dp"
        android:textAlignment="center"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <EditText
        android:id="@+id/signuppasswotd"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp"
        android:textAlignment="center"
        android:layout_marginRight="100dp"
        android:textSize="30dp"
        android:hint="password"
        android:layout_below="@id/signupusername"
        android:inputType="textPassword" />

    <EditText
        android:id="@+id/comfirmpassword"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/signuppasswotd"
        android:layout_marginTop="20dp"
        android:layout_centerHorizontal="true"
        android:layout_marginRight="100dp"
        android:textAlignment="center"
        android:hint="confirm password"
        android:textSize="30dp"
        android:inputType="textPassword" />

    <Spinner
        android:id="@+id/spinner"
        android:layout_width="wrap_content"
        android:layout_marginTop="20dp"
        android:layout_alignStart="@id/comfirmpassword"
        android:layout_alignEnd="@id/comfirmpassword"
        android:layout_below="@id/comfirmpassword"
        android:layout_height="wrap_content" />

    <Button
        android:id="@+id/signupsubmit"
        android:layout_width="200dp"
        android:layout_height="60dp"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="40dp"
        android:theme="@style/orangebutton"
        android:textSize="20dp"
        android:layout_below="@id/spinner"
        android:text="submit" />
</RelativeLayout>